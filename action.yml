name: z-os toolchain
description: Use the z-os toolchain
inputs:
  script:
    description: Script to run
    required: true
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.script }}
