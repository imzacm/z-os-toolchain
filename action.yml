name: z-os toolchain
description: Use the z-os toolchain
inputs:
  script:
    description: Script to run
    required: true
runs:
  using: docker
  image: imzacm/z-os-toolchain:v0.1.2
  args:
    - ${{ inputs.script }}
